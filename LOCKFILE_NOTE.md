# package-lock

`package-lock.json` on `main` (and this branch, branched from main) is the full lockfile (lockfileVersion 3).

Use `npm ci` / `npm install` for reproducible installs. Vercel: Install command `npm install` (see DEPLOY.md).
