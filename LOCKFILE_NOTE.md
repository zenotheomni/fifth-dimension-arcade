# package-lock

`package-lock.json` on this branch is the full lockfile (lockfileVersion 3) generated with `npm install` from `package.json` on the builder box.

Use `npm ci` / `npm install` for reproducible installs. Vercel: Install command `npm install` (see DEPLOY.md).
