import PlaceholderShell from "@/components/PlaceholderShell";

export default function KeyPage() {
  return (
    <PlaceholderShell
      ticket="NFC Key"
      title="Key"
      copy="NFC landing placeholder — ships in M5. Run it back."
      event="arcade_key_nfc_shell_view"
      ctaLabel="Enter The Fifth Floor"
    />
  );
}
